# Environment & secrets — never commit these
.env
.env.local
*.pem
*.key
wallets.json                   # contains private keys — use wallets.example.json as template
circle_config.json             # contains Circle API key — use circle_config.example.json as template

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
dist/
build/
*.egg-info/

# Node (if frontend added later)
node_modules/
.next/

# Hardhat / Foundry artifacts
artifacts/
cache/
out/

# Streamlit
.streamlit/secrets.toml

# OS
.DS_Store
Thumbs.db
